SDES Project 1:

The source folder consists of python code which runs for the under damped, critically damped and over damped conditions, a make file by running in the command will run the python file and send the results to the output folder.

The output can be cleaned by running the clean command in the terminal.

The output folder consists of the report, animated videos and the html file of the ipythoon notebook.
